---
type: Concept
category: concept
title: "Generated Artifacts"
description: "The seven generated outputs the runtime loads instead of source (core_compiled.lgb, core_go_lowered/, the IR op and data tables, the primitive registrars), the content-hash manifest that decides what is stale, and the bootstrap build tag that keeps regeneration independent of the running binary."
tags: [runtime, compiler, tooling, go, bytecode]
resource: "https://github.com/nooga/let-go/blob/main/docs/regenerating-generated-artifacts.md"
sources:
  - "doc: nooga/let-go docs/regenerating-generated-artifacts.md @ 0911118, 2026-09-05"
  - "repo: nooga/let-go pkg/genmanifest/{manifest,depmanifest,tree}.go, pkg/rt/generated.manifest, scripts/generate.lg, cmd/lgbgen/{main,generate,main_stub}.go, pkg/rt/{core_embed,core_embed_bootstrap}.go, Makefile @ 0911118, 2026-09-05"
  - "pr: nooga/let-go#641 (provenance manifest + selective regeneration), #557 (gogen embedded outside the bootstrap universe); nnunley's architecture decision on #735 (2026-09-02), 2026-09-05"
created: "2026-09-05"
updated: "2026-09-05"
status: stable
---

# Generated Artifacts

The runtime does not compile `core.lg` at startup. It loads committed artifacts generated from the `.lg` sources, and a Go build cannot regenerate them: Go has no build-time codegen hook, so an edit under `pkg/rt/core/` has no effect until `make generate` runs. Everything on this page exists to make that fact safe: a manifest that says which artifact is stale, gates that refuse to build or commit against a stale one, and a build tag that lets the generator compile core from source without trusting the bundle inside the binary that runs it.

## The outputs

`pkg/rt/generated.manifest` declares seven outputs. Six are committed; the lowered Go tree is gitignored and rebuilt locally. Each is regenerated by a stage of `scripts/generate.lg`, run as `make generate`.

| Output | Generated from | Loaded by |
|---|---|---|
| `pkg/rt/core_compiled.lgb` | every embedded `.lg` under `pkg/rt/core/`, compiled by `cmd/lgbgen` | the default runtime, at boot (see [runtime image](runtime-image.md)) |
| `pkg/rt/core_go_lowered/` (gitignored) | the same sources, lowered to Go by `cmd/lgbgen --target=go` | builds with `-tags gogen_ir` (see [compile paths](compile-paths.md)) |
| `pkg/ir/op_generated.go` | `pkg/ir/ir_ops.lg` via `scripts/generate.lg` | the Go side of the [IR](indexed-rpn-ir.md): op table and catalog |
| `pkg/rt/ir_bridge_generated.go` | `pkg/ir/ir_bridge.lg` | the Go bridge primitives the [IR pipeline](ir-pipeline.md) calls |
| `pkg/rt/core/ir/data/generated.lg` | `pkg/ir/ir_data.lg` | the IR data accessors, on the Lisp side |
| `pkg/rt/zz_primitives_generated.go` | `//lg:native` annotations in `pkg/rt` via `cmd/lgprimgen` | the runtime's own primitive registrar (see [native primitives](native-primitives.md)) |
| `pkg/rt/corefns/zz_primitives_generated.go` | annotations in `pkg/rt/corefns` | the contribute-mode registrar |

The three IR generators run in process: `generate.lg` `load-string`s each `.lg` generator, capturing stdout with `with-out-str` for the op and bridge tables and writing the return value of the data generator's final form, because shelling out to a second `lg` dropped trailing bytes nondeterministically. It shells out to `lgprimgen` for the two registrars and once to `go run -tags bootstrap ./cmd/lgbgen --target=both`, one core compile feeding both the bundle and the lowered tree so the untagged and `gogen_ir` runtimes cannot drift apart.

## Staleness is content, not time

`make build` and the older `check-*-fresh` targets compared modification times, and mtimes lie after a `git` or `jj` checkout: the VCS writes arbitrary times, so a stale bundle can look newer than the source that should have rebuilt it. `pkg/genmanifest` hashes content instead.

The manifest is a list of edges, one per line: `<output> <input> <kind> <sha256>`, where `kind` is `input` (a domain source such as `ir_ops.lg`), `generator` (a file of the generator's own implementation), or `output` (the declared output's last generated bytes). `pkg/rt/generated.sums` holds a single digest of the manifest and is the value the gates compare. Generator edges come from `go list -deps -json -tags bootstrap ./cmd/lgbgen`, so a change to the bytecode encoder or the compiler counts as a reason to regenerate the bundle, and the manifest lists every non-test build source across platform and tag variants so it is stable on Linux, macOS, and Windows (#641).

Two separate questions are asked of it:

- **Are the inputs fresh?** A recorded input or generator digest that differs from the file on disk means someone edited a source without regenerating.
- **Is the output ready?** For a file output, do its bytes still match the recorded output edge; for the lowered tree, is the sentinel present. `cmd/lgbgen` writes `pkg/rt/core_go_lowered/`'s tree manifest as the last step of a successful install and deletes it first on reinstall, so a crash mid-install can never leave a valid sentinel over a torn tree.

`make generate` reruns only stages whose inputs changed or whose outputs are disturbed, and requeries staleness after the IR generators run so a change that propagates into `lgbgen`'s inputs is picked up in the same pass. A staleness query that fails is an error, not an empty clean set.

## Where a stale artifact is caught

| Gate | What it does |
|---|---|
| `make check-generated-manifest` | non-mutating front gate: fails on a manifest mismatch before any target could refresh the digest |
| `make check-generated` | runs the front gate, then regenerates the registrars and bundle in place and `cmp`s them against saved copies of the committed files; a failing run leaves the regenerated bytes in the tree |
| `go test ./...` | `TestGeneratedArtifactsAreFresh` in `pkg/genmanifest` fails when a source changed without `make generate` |
| pre-commit hook | `scripts/pre-commit` blocks a commit with stale artifacts once installed; `jj` runs no git hooks, so `jj` users rely on the test and the make target |
| merge drivers | `make install-hooks` registers `merge.lgb` and `merge.sums`, which regenerate `core_compiled.lgb` and recompute `generated.sums` after the `.lg` sources have merged as text, so a rebase that touches `core.lg` does not end in a binary conflict |

The merge drivers live in `.git/config`, registered by `make install-hooks`; the pre-commit hook is a manual symlink into `.git/hooks/`. Neither is shared, so each clone sets them up once.

## The bootstrap tag

`cmd/lgbgen` is built with `-tags bootstrap`. Under that tag `pkg/rt/core_embed_bootstrap.go` replaces the `//go:embed core_compiled.lgb` in `core_embed.go`, so the generator compiles core from source and never consults the bundle embedded in whatever `lg` is running the build. A stale embedded bundle therefore cannot taint regeneration. `scripts/generate.lg` itself runs on `./lg`, the bundle-built binary, and shells out to the bootstrap build for the one heavy stage; without the tag `cmd/lgbgen` is a stub.

The embedded-core tree (`//go:embed all:core`) is also the universe the self-hosting bootstrap walks when it derives the Go-lowering order, topologically sorting every `(ns ...)` by its `:require` edges plus one synthetic edge: `ir.build` must load after every optimization and inference pass, because the passes register state at load time and lowering before that state exists emits untyped arithmetic. Adding a source to that tree changes what the bootstrap lowers. That is why `gogen`, the Go-emission macro layer the AOT path requires, is embedded from `pkg/rt/gogen/` through a separate `//go:embed` gated `!bootstrap` and served as an auxiliary embedded source (#557): a shipped `lg` can `(require 'gogen)` from any working directory, while the bootstrap resolves it from the classpath as before, keeping cold and hot lowering byte-identical. The exception is `gogen`'s alone and transitional. Norman's decision on #735 (2026-09-02) is that the compiler orchestration namespace, `lg.compiler`, enrolls in the standard embedded and generated set immediately (`EmbeddedNSNames` plus the genmanifest source specs) with no temporary auxiliary embed, and his proposal for the compiler namespaces (#786) names moving `gogen` itself into the bootstrap universe as a step that must first resolve that fixpoint rather than simply enroll the file.

## Working with them

- Edit anything under `pkg/rt/core/**/*.lg`, `pkg/ir/ir_*.lg`, or an annotated Go primitive, then `make generate` and commit the regenerated committed outputs (bundle, sums, manifest, registrars, IR tables) with the change; the lowered tree stays local. `make check-generated` before pushing.
- A PR that regenerates `core_compiled.lgb` or `generated.sums` will conflict with any other such PR on rebase; regenerate after the text merge rather than resolving the binary or the digest by hand.
- `go generate ./cmd/lgbgen` runs the two `//go:generate` directives (bundle, then `--target=go`); prefer `make generate`, which also runs the IR generators and the registrars and passes the exact resolved `go` and `lg`, recording both in a local provenance file (`pkg/rt/generated.provenance`, gitignored) and reporting `lg` version drift from the previous run in the build log (the Go version is recorded but not compared).

## Citations

**Resource:** [docs/regenerating-generated-artifacts.md](https://github.com/nooga/let-go/blob/main/docs/regenerating-generated-artifacts.md): the contributor-facing rules  
**Related:**
- [pkg/genmanifest/manifest.go](https://github.com/nooga/let-go/blob/main/pkg/genmanifest/manifest.go), [depmanifest.go](https://github.com/nooga/let-go/blob/main/pkg/genmanifest/depmanifest.go), [tree.go](https://github.com/nooga/let-go/blob/main/pkg/genmanifest/tree.go): content hashing, edge kinds, the lowered-tree sentinel
- [scripts/generate.lg](https://github.com/nooga/let-go/blob/main/scripts/generate.lg): the orchestrator, in-process generators, provenance record
- [cmd/lgbgen/main.go](https://github.com/nooga/let-go/blob/main/cmd/lgbgen/main.go) and [generate.go](https://github.com/nooga/let-go/blob/main/cmd/lgbgen/generate.go): the bootstrap compiler, lowering-order derivation, `//go:generate` directives
- [pkg/rt/core_embed.go](https://github.com/nooga/let-go/blob/main/pkg/rt/core_embed.go) and [core_embed_bootstrap.go](https://github.com/nooga/let-go/blob/main/pkg/rt/core_embed_bootstrap.go): the tag-gated embed
- [Makefile](https://github.com/nooga/let-go/blob/main/Makefile): `generate`, `check-generated`, `install-hooks`
- PRs: [#641](https://github.com/nooga/let-go/pull/641) provenance manifest and selective regeneration, [#557](https://github.com/nooga/let-go/pull/557) gogen self-containment; issue [#786](https://github.com/nooga/let-go/issues/786) compiler namespace consolidation

See also: [.lgb Bytecode Format](lgb-bytecode-format.md), [Runtime Image](runtime-image.md), [lg-compile](lg-compile.md), [let-go](../entities/let-go.md)
